{
  "name": "@serverless-lambda-python-monorepo/second",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy": "virtualenv venv --python=python3 && source venv/bin/activate && serverless deploy",
    "remove": "serverless remove",
    "test": "virtualenv venv --python=python3 && source venv/bin/activate && poetry install && python -m unittest discover"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@serverless-lambda-python-monorepo/pycommon": "1.0.0"
  }
}
